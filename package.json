{
  "name": "expense-tracking-agent",
  "version": "1.0.0",
  "description": "ExpenseAI - Full stack expense tracker: React + Tailwind + Express + MongoDB",
  "scripts": {
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "dev": "start cmd /k \"cd server && npm run dev\" && cd client && npm run dev"
  },
  "readme": "See README.md for setup instructions"
}
